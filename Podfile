# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'News_Swift' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for News_Swift

  target 'News_SwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'News_SwiftUITests' do
    # Pods for testing
  end

pod 'Alamofire', '~> 5.2'
pod 'SDWebImage', :modular_headers => true
pod 'Loading'

end
